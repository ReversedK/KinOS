node_modules/
dist/
.next/
next-env.d.ts
*.tsbuildinfo
.DS_Store
*.log

# Local data (SQLite stores etc.) must never be committed.
*.sqlite
*.sqlite3
data/

# Credentials / secrets must never be committed (secret-store references only).
google-credentials.json
*-credentials.json
*.pem
.env
